%2_compressor.m

%  dynamic compressor with the following input
% arguments: threshold, slope, optional gain matching, and input signal. Plot the
% original signal, compressed signal, the difference between the signals, the envelopes,
% and show how the two signals differ spectrally